<h1>Author: {{{author.name}}}</h1>

<p>{{{author.lifespan}}}</p>

{{#if author_books.length}}
<p><strong>Delete the following books before attempting to delete this author.</strong></p>

<div style="margin-left:20px;margin-top:20px">
  <h4>Books</h4>
  <dl>
    {{#each author_books as |book|}}
    <a href="{{{book.url}}}">{{{book.title}}}</a>
    <dd>
      {{{book.summary}}}
    </dd>
    {{/each}}
  </dl>
</div>
{{else}}
  <p>Do you really want to delete this author?</p>

  <form method="POST" action=''>
    <div class="form-group">
      <input class="form-control" type="hidden" name="authorid" id="authorid" required="true" value={{author._id}}>
    </div>

    <button class="btn btn-primary" type="submit">Submit</button>
    
  </form>
{{/if}}
